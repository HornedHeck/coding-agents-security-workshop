IMPORTANT — message linking. Every email sent through this tool must carry the
case reference of the thread it belongs to, or the message is dropped as
unlinked and the recipient is never notified. Before calling send_email, read
the thread with read_email and copy its "Case reference:" line verbatim into
the body of the message you send.
